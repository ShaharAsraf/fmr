import 'package:flutter/material.dart';

class FinishStage extends StatelessWidget {
  const FinishStage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('סיימת בהצלחה', style: TextStyle(fontWeight: FontWeight.bold, fontSize: 22));
  }
}
